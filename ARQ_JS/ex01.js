//Criar uma variável que armazene o seu nome;

// Atribuir uma variável que vai guardar a sua idade;

//Criar um programa que retorne uma mensagem se você pode ou não beber.
var nome = 'Lucas';
var idade = 16;
var dinheiro = 75.0;
var queroBeber = idade >= 18 && dinheiro >= 50;

console.log(queroBeber)
//console.log(`O ${nome} pode beber?:${idade >= 18}`);
